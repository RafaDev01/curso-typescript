"use strict";
let numeros = [20, 30, 40];
numeros.push(10);
numeros.unshift(50);
numeros.pop();
numeros.shift();
console.log(numeros);
let numero_ro = [20, 30, 40, 1000, 10000];
console.log(numero_ro);
